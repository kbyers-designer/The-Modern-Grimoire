<!DOCTYPE html>

<html lang="en" class="dark">
<head>
<meta charset="utf-8"/>
<meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no"/>
<title>The Modern Grimoire</title>
<script src="https://unpkg.com/react@18/umd/react.production.min.js"></script>
<script src="https://unpkg.com/react-dom@18/umd/react-dom.production.min.js"></script>
<script src="https://unpkg.com/@babel/standalone/babel.min.js"></script>
<link href="https://fonts.googleapis.com/css2?family=Noto+Serif:ital,wght@0,400;0,700;1,400&family=Manrope:wght@300;400;500;600;700;800&display=swap" rel="stylesheet"/>
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet"/>
<style>
*,*::before,*::after{box-sizing:border-box;margin:0;padding:0}
:root{
  --bg:#121319;--surface:#121319;--surface-low:#1a1b22;--surface-container:#1e1f26;
  --surface-high:#292a30;--surface-highest:#34343b;--surface-bright:#383940;
  --surface-lowest:#0d0e14;--surface-variant:rgba(52,54,59,0.4);
  --on-surface:#e3e1ea;--on-surface-variant:#c7c5cc;--tertiary:#e9c176;
  --on-tertiary:#412d00;--outline-variant:#46464c;--tertiary-container:#1e1300;
  --primary:#c4c5db;
}
html{scroll-behavior:smooth}
body{background:var(--bg);color:var(--on-surface);font-family:'Manrope',sans-serif;
  min-height:100dvh;overflow-x:hidden;-webkit-font-smoothing:antialiased}
.font-headline{font-family:'Noto Serif',serif}
.font-label{font-family:'Manrope',sans-serif}
.material-symbols-outlined{font-variation-settings:'FILL' 0,'wght' 300,'GRAD' 0,'opsz' 24}
.material-symbols-outlined.filled{font-variation-settings:'FILL' 1,'wght' 300,'GRAD' 0,'opsz' 24}

/* Scrollbar */
::-webkit-scrollbar{width:4px}
::-webkit-scrollbar-track{background:var(–bg)}
::-webkit-scrollbar-thumb{background:var(–outline-variant)}

/* Animations */
@keyframes fadeUp{from{opacity:0;transform:translateY(20px)}to{opacity:1;transform:translateY(0)}}
@keyframes fadeIn{from{opacity:0}to{opacity:1}}
@keyframes pulseGlow{0%,100%{box-shadow:0 0 15px rgba(233,193,118,0.2)}50%{box-shadow:0 0 30px rgba(233,193,118,0.4)}}
@keyframes breathe{0%,100%{transform:scale(1)}50%{transform:scale(1.02)}}
.animate-fadeUp{animation:fadeUp .6s ease-out both}
.animate-fadeIn{animation:fadeIn .4s ease-out both}
.delay-1{animation-delay:.1s}.delay-2{animation-delay:.2s}.delay-3{animation-delay:.3s}
.delay-4{animation-delay:.4s}.delay-5{animation-delay:.5s}

/* Timer ring animation */
@keyframes timerSpin{from{stroke-dashoffset:283}to{stroke-dashoffset:0}}
.timer-ring{stroke-dasharray:283;transform:rotate(-90deg);transform-origin:50% 50%;transition:stroke-dashoffset 1s linear}
.timer-active{animation:pulseGlow 2s ease-in-out infinite}

/* Page transitions */
.page-enter{animation:fadeUp .4s ease-out both}
</style>

</head>
<body>
<div id="root"></div>
<script type="text/babel">
const {useState, useEffect, useRef, useCallback} = React;

// ─── Image URLs (from original designs) ───
const IMG = {
hero: “https://lh3.googleusercontent.com/aida-public/AB6AXuDche_LNDv0fnckdDdbCUo2oLIsrNeAwDiaUXlziFPIwYNEm4O91FqqWrJfZLW9A9vmvMOsduRdUSmHbxTVx9yHdtcPYfRjqeRS9vfySbIInFPfYwoBoucMkNI28yKEsCm14b1sdToznenCnHsOnRO0uKzP56DY1YMbILpzGMXky1IOlDrUiYW5iIfBrR3_rKjpnJLje43xRumeUXf7TXElvMPfDUQWmT38TUAZsZHm1vgAQkDjwR_3cvMN2d_DkrzPS5x2qG8EnEQ”,
spells: “https://lh3.googleusercontent.com/aida-public/AB6AXuBquZ3mWXh0FgZQnLueE8VkpiEvhbei-JZbN7vNi0TCxWc7PfshQgaAX4bal67uZlvsnYOVGPt8dg9giEEmod_p9jmylzxz4l4SUFMrDwKb65w9xtMeG9GCm4m-w8yvbzGwwPMv3VMC0H05zBEmigIKlT_T73t2Ppm0jbpkxrUMZMdNeqyYq-5TvaqkW9SRZK0OgvBDuQ7tvBuhGFfP–gDrUUDVnu9KdOMGVQa0Py8_b0qgGWTjDM0ctYCrWva8Ylw9oJzCCCOWkc”,
crystals: “https://lh3.googleusercontent.com/aida-public/AB6AXuBisE9bKoAM7NhI2b6HtLVQJqWemNBNlutmM45ICe-83R7xNd5XSO4sDRCo9uPMVmkDtf7VNoDCk62icabPUSJk756EICBgsKI2nSRj66vDWokKdHDG702NMuZmuNLVcXgWaJknK0ymv66C3DS74VJ6P8_1UPZ_nO5I6DgVEN268wd7csAqpaqdDFtVvOqa8KGloUHJS9YvK1kf8H11FpS-mmg4_PLuQ-edTtXlmcQ6mtdKh1EdjXa6Cyk337Mp1V4hNXhSuNv-nMM”,
herbs: “https://lh3.googleusercontent.com/aida-public/AB6AXuCtUGxaNwDCnPbO4kLgzuPuQcyHDXjvVLHx0N-wx_qQHz04i6neHuP0JZvOjgvfMVGq0jekhG6nO7ld6R4JW_EyZkla3JXLYDoI3atI4R9_FUpbKmLU2V9dO4pXIGeK29yYetNsR7QzfF_ulhwd_BpFQRimJIeurWb_EA8ZW1lnM2GHZZ5mR5n-LfAYtQS55qRdB2NWE_CkquT-RvPCG5p-wvi17dcYL7mp-ept6MFsK_BNXzgRDywN97lYnxysiDXdNFiaynTVtPQ”,
amethyst: “https://lh3.googleusercontent.com/aida-public/AB6AXuDc0MDo8bg1zsrUQ9EWO85RSRlEIH4IQxsknv5k2Ujb3pSSrEoRM4w7ftJU3nQFKst3rf8TQ3bsE3yBGmiTWpdvT5QbB_hVxYAUXoWh0PCW5hoWOLAk-EKrbrXFaY2Z7A9Wzki8KSKgFyxPQjTb7iwpMNpcQsjyUygE7VumJFnxAIYzCKmKWMmqyYEtXISAgi76WiAgCpNKYx8AZbOnoy0SHlts6J5kd0T_qtsubxVlQYTQRXx5piVOT6v5ot1a6sv0QBCOnoBmLlk”,
lavender: “https://lh3.googleusercontent.com/aida-public/AB6AXuCs_WJP6qr-N1EWHW90tTB5ZDVlbZjX22l2EySxnDTOeo64SVAqxdS2v0Pwzcyv3gL3uN6sl05NuHZey_ZBV4nBMvlwKqWS2SxyDczV_XnHDLWIf27lgiVcssFdNkz8LCzU_EKy-gzAgQgVurOM06-LVVfJsgYNvIjyHAl5RieYy19UqpQpfRD0b4NnzL95GVQHVBikHeUCsvGWara8m9_U0Kjl7zz5NDx4D-dwzNYNfNwrxYH5PnZm9hGGOl5NxU28FTWdwCN_ZZU”,
mugwort: “https://lh3.googleusercontent.com/aida-public/AB6AXuB02CCnITSxrM7cd1auZjfkj7lcKOmQLFegPe-i13YU-pMcDT8F3LmfKv4pQ8RJayDM_W1kuD5PKTA6P-vhSxPOOBE5NqfdkYd0dV6MTXwPhRtYLZjw0_AE7acqU07eBA1OWqvIaZ8LkucIrsEchjrxwa3Lx-iw9YXJo0GiG6SrCnDI3s06cGKIEdlKy1gKpeaGXegAxchoYVFr26xiCL82vnlWj_FPzf9MdRVqLtCGvzRTJpoU7I_WWB1VwGqon4m5r1_TybaSWFE”,
citrine: “https://lh3.googleusercontent.com/aida-public/AB6AXuAZbTZLvSDlYBNZaUhbSEnrulW7-sGLn24CNlVwq5Ms7kdDNtY3ktbt5QBpxV-3TKs9hb2l7lq61reMQkUPNOIrtuPyGoIvte_9pSW8OOQi4MD9NMRWRqIaYvCYWiTHNEYbrdpbFDnKDvatb4FTZi070-4mTyHmQoTZ1SVbPug_w-nkKfPD2S9CN7z1I6SeswcwYKjZANlGsUy88mkIkOdy-hodeG5exHszYqHHfr12RvQ1CBLVr9Yf7EFkAy3aTCXTR4wy9gPluMg”,
rosemary: “https://lh3.googleusercontent.com/aida-public/AB6AXuDokfcGXC4qL86v4IXKAVY3IT2TbO8cxjqdILevWd8ZIhQYJlK6YmnbDojwtiL3dDCSa4yEpuib9RWn295nRfp_mFCcmHxNnNEk99xsJs4NukJYQKO3yfMrpcL1WDNbehSkR6qzVddorCx9Bo4LDQnz9Dq9QmAgJk85JvKiXRayT1f-2qWvcY0J88663eOmyD4CBnsfFDSZJ5_42Yhiszrzcd7XRaloKPy_88QSBAsZtXhJuk-hGjJnngEOpm82_UId8XAEgEM9fa8”,
moonstone: “https://lh3.googleusercontent.com/aida-public/AB6AXuC-PPqmsO9aAr704z8sN-bYQECjUyyG6djUHD-NrBTnHfoVGPtHcWbi5HQMqNZQ_-wb5koDtm-gI2OQWWLswvGfd_GpMRbmW_AkTxJji1EDkoRhYXyiRv7K-zqFJBi0uTZ3bp9FYhySjw9sPNGLQ-lcCd2HXnwVw3mVWP0EBGpJsioTh7603H7ap4B4jmoQa8GOC9rGaF8GiW9Nbj5VtYBZ-qfrMTHebiDPmB7KOpxtqppQigxpuGl46bATJpaFuRTN3aD8MOKFxFc”,
moon: “https://lh3.googleusercontent.com/aida-public/AB6AXuCmKESE_VWMBmmhjTwhKa8v9ur6AxwbvzeIoZW0XssKlFZQlBXs-JYvUM7JcVaZkSqUg5ByrLGls-uflr3fk1w8DbaOZEdaxGu4VyhK0-defRUwl6d9nGf4efKmEYpnUQ85Sc2gjWq6bx-OOdfqniru2Zn2xUElALQOqR8COcExPoxKFbNabB1EnTxpTWgK0ElXoL59l90nqj04Jw_40ZOMILCmnDpd-nbPgbQ9ufMngZyA3evCfpqmH7K2c5KWdxXEPnbT3E9U8V8”,
sage: “https://lh3.googleusercontent.com/aida-public/AB6AXuCWOsXd2t2p2heJD65-ljW-CfvsD0Vpsw5OtVv4nQT8IztjebHWz3Y_J8I6ufJaVkpUMbyEsp30s68BWFE0Nt-6JQFRHlrceKWEXt27FfMaHrUqFEXTkJ1h6Uj8s7bGto6Re9erHuUcx6fjFo2tNkO1PiN3wwe8sUlKM9V69y_1SPBoXM5VhEjliPGzgrXVKd9xX8Uoik7qyS0FRZU_7hum1gRQVMKAsaTfyCOh3rRBLBoUo0bN-DPLKdr29a8WanGVRHT6ktlpXEw”,
altar: “https://lh3.googleusercontent.com/aida-public/AB6AXuC_sWgKnAfX2NhCQtt8Tv9Yw0OmHP4qLih5OLH4l1H6YIE_xKWVPksHpCkwO25JPnBPHLJliNniXifTKGTeb4TohkoRZfo3b62F58gWN8Fxv0j80S8sPxoVpIx6Toit9-fHqyMVvdpFs1opdpVARrdoeAfP9tpUBgTmKuoI3V9BsOeoN1fSNm0Bt0WmumiYFncAJ1hz8Ll6VMq8L5WhHC05e4glvjZtgneDgu4jH0t2MroOJpye000OJPEVqC2NBhWiTUQ-e4gycHg”,
};

// ─── Icon component ───
const Icon = ({name, filled, className=””}) => (
<span className={`material-symbols-outlined ${filled?'filled':''} ${className}`}>{name}</span>
);

// ─── Header ───
const Header = ({onSearch}) => (

  <header style={{
    position:'fixed',top:0,left:0,right:0,zIndex:50,
    background:'linear-gradient(to bottom, #1a1b22, transparent)',
    padding:'16px 24px',display:'flex',justifyContent:'space-between',alignItems:'center'
  }}>
    <div style={{display:'flex',alignItems:'center',gap:12}}>
      <Icon name="menu_book" className="filled" style={{color:'var(--tertiary)',fontSize:28}}/>
      <h1 className="font-headline" style={{fontSize:22,letterSpacing:'-0.02em',color:'var(--on-surface)'}}>The Modern Grimoire</h1>
    </div>
    <button onClick={onSearch} style={{background:'none',border:'none',padding:8,cursor:'pointer',borderRadius:4}}
      onMouseOver={e=>e.target.closest('button').style.background='var(--surface-highest)'}
      onMouseOut={e=>e.target.closest('button').style.background='none'}>
      <Icon name="search" style={{color:'rgba(227,225,234,0.6)'}}/>
    </button>
  </header>
);

// ─── Bottom Nav ───
const BottomNav = ({active, onChange}) => {
const tabs = [
{id:‘library’,icon:‘auto_stories’,label:‘Library’},
{id:‘rituals’,icon:‘auto_fix_high’,label:‘Rituals’},
{id:‘correspondence’,icon:‘menu_book’,label:‘Index’},
{id:‘guides’,icon:‘explore’,label:‘Guides’},
];
return (
<nav style={{
position:‘fixed’,bottom:0,left:0,right:0,zIndex:50,height:72,
display:‘flex’,justifyContent:‘space-around’,alignItems:‘center’,
background:‘rgba(18,19,25,0.92)’,backdropFilter:‘blur(24px)’,
borderTop:‘1px solid rgba(70,70,76,0.15)’,
boxShadow:‘0 -8px 32px rgba(0,0,0,0.4)’
}}>
{tabs.map(t=>(
<button key={t.id} onClick={()=>onChange(t.id)} style={{
background:‘none’,border:‘none’,cursor:‘pointer’,display:‘flex’,flexDirection:‘column’,
alignItems:‘center’,gap:4,color:active===t.id?‘var(–tertiary)’:‘rgba(227,225,234,0.5)’,
transition:‘all .3s’,transform:active===t.id?‘scale(1.05)’:‘scale(1)’,
filter:active===t.id?‘drop-shadow(0 0 8px rgba(233,193,118,0.3))’:‘none’
}}>
<Icon name={t.icon}/>
<span className=“font-label” style={{fontSize:10,textTransform:‘uppercase’,letterSpacing:‘0.05em’}}>{t.label}</span>
</button>
))}
</nav>
);
};

// ─── LIBRARY PAGE ───
const LibraryPage = ({onNavigate}) => (

  <div className="page-enter" style={{paddingTop:72,paddingBottom:100}}>
    {/* Hero */}
    <section style={{padding:'0 24px',marginBottom:48}}>
      <div style={{position:'relative'}}>
        <div style={{position:'absolute',top:-80,left:-80,width:300,height:300,
          background:'radial-gradient(circle,rgba(233,193,118,0.08) 0%,transparent 70%)',pointerEvents:'none'}}/>
        <span className="font-label" style={{fontSize:10,textTransform:'uppercase',letterSpacing:'0.2em',color:'var(--tertiary)',display:'block',marginBottom:16}}>Current Focus</span>
        <h2 className="font-headline animate-fadeUp" style={{fontSize:'clamp(40px,8vw,64px)',lineHeight:1.05,marginBottom:24,letterSpacing:'-0.02em'}}>
          Ritual of the <br/><em style={{color:'rgba(227,225,234,0.8)'}}>Celestial Equinox</em>
        </h2>
      </div>
      <div className="animate-fadeUp delay-1" style={{position:'relative',overflow:'hidden',borderRadius:2}}>
        <img src={IMG.hero} alt="" style={{width:'100%',height:380,objectFit:'cover',filter:'brightness(0.75) grayscale(0.2)'}}/>
        <div style={{position:'absolute',inset:0,background:'linear-gradient(to top,var(--bg),transparent 60%)'}}/>
        <div style={{position:'absolute',bottom:24,left:24,right:24}}>
          <p style={{color:'rgba(227,225,234,0.7)',marginBottom:20,lineHeight:1.6,maxWidth:400}}>
            Aligning your inner frequency with the balance of the shifting seasons. Requires Lavender, Clear Quartz, and focus.
          </p>
          <button onClick={()=>onNavigate('rituals')} style={{
            background:'var(--tertiary)',color:'var(--on-tertiary)',border:'none',padding:'12px 32px',
            cursor:'pointer',fontSize:11,textTransform:'uppercase',letterSpacing:'0.15em',fontFamily:'Manrope',fontWeight:600,
            boxShadow:'0 0 15px rgba(233,193,118,0.2)',transition:'all .3s'
          }}>Begin Ritual</button>
        </div>
      </div>
    </section>

```
{/* Moon Phase + Daily Insight */}
<section style={{padding:'0 24px',marginBottom:48,display:'flex',flexDirection:'column',gap:16}}>
  <div className="animate-fadeUp delay-2" style={{background:'var(--surface-low)',padding:28,borderLeft:'2px solid rgba(233,193,118,0.2)'}}>
    <span className="font-label" style={{fontSize:10,textTransform:'uppercase',letterSpacing:'0.1em',color:'rgba(233,193,118,0.6)',display:'block',marginBottom:8}}>Phase</span>
    <h3 className="font-headline" style={{fontSize:22,marginBottom:12}}>Waxing Gibbous</h3>
    <p style={{fontSize:14,color:'rgba(227,225,234,0.5)',lineHeight:1.6,fontStyle:'italic'}}>An auspicious time for growth, manifestation, and the gathering of power.</p>
  </div>
  <div className="animate-fadeUp delay-3" style={{background:'var(--surface-highest)',padding:28,position:'relative',overflow:'hidden',transition:'background .3s'}}>
    <span className="font-label" style={{fontSize:10,textTransform:'uppercase',letterSpacing:'0.1em',color:'var(--tertiary)',display:'block',marginBottom:12}}>Daily Insight</span>
    <p className="font-headline" style={{fontSize:18,fontStyle:'italic',lineHeight:1.5}}>"The stars only reveal what the soul is ready to perceive."</p>
  </div>
</section>

{/* Collections */}
<section style={{padding:'0 24px',marginBottom:48}}>
  <div style={{display:'flex',justifyContent:'space-between',alignItems:'flex-end',marginBottom:24}}>
    <h3 className="font-headline" style={{fontSize:28}}>The Collections</h3>
    <span className="font-label" style={{fontSize:10,textTransform:'uppercase',letterSpacing:'0.1em',color:'rgba(233,193,118,0.6)'}}>View All Wisdom</span>
  </div>
  <div style={{display:'flex',flexDirection:'column',gap:16}}>
    {[
      {name:'Spells',img:IMG.spells,icon:'auto_fix_high',sub:'Incantations for the modern seeker. 142 entries.',big:true},
      {name:'Crystals',img:IMG.crystals,icon:'diamond',sub:'Vibrational conduits. 89 entries.'},
      {name:'Herbs',img:IMG.herbs,icon:'eco',sub:'Botanical alchemy. 115 entries.'},
    ].map((c,i)=>(
      <div key={c.name} className={`animate-fadeUp delay-${i+2}`} onClick={()=>onNavigate('correspondence')}
        style={{position:'relative',overflow:'hidden',height:c.big?220:180,background:'var(--surface-low)',cursor:'pointer'}}>
        <img src={c.img} alt="" style={{position:'absolute',inset:0,width:'100%',height:'100%',objectFit:'cover',
          opacity:c.name==='Crystals'?0.3:0.4,filter:c.name==='Crystals'?'grayscale(1)':'none',transition:'transform .7s'}}/>
        <div style={{position:'relative',height:'100%',padding:24,display:'flex',flexDirection:'column',justifyContent:'space-between'}}>
          <div>
            <Icon name={c.icon} style={{color:'var(--tertiary)',fontSize:28,display:'block',marginBottom:12}}/>
            <h4 className="font-headline" style={{fontSize:c.big?32:26}}>{c.name}</h4>
          </div>
          <p className="font-label" style={{fontSize:10,textTransform:'uppercase',letterSpacing:'0.1em',color:'rgba(227,225,234,0.6)',maxWidth:300}}>{c.sub}</p>
        </div>
      </div>
    ))}

    {/* Correspondences card */}
    <div className="animate-fadeUp delay-5" onClick={()=>onNavigate('correspondence')}
      style={{position:'relative',overflow:'hidden',padding:32,background:'var(--surface-low)',cursor:'pointer'}}>
      <div style={{position:'absolute',inset:0,background:'radial-gradient(circle,rgba(233,193,118,0.08) 0%,transparent 70%)',opacity:0.2}}/>
      <div style={{position:'relative'}}>
        <Icon name="join_inner" style={{color:'var(--tertiary)',fontSize:28,display:'block',marginBottom:12}}/>
        <h4 className="font-headline" style={{fontSize:32,marginBottom:12}}>Correspondences</h4>
        <p style={{color:'rgba(227,225,234,0.6)',lineHeight:1.6,maxWidth:400}}>Map the intricate links between planets, days, colors, and intent to amplify your manifestations.</p>
      </div>
    </div>
  </div>
</section>

{/* Recently Consulted */}
<section style={{padding:'0 24px'}}>
  <div style={{display:'flex',alignItems:'center',gap:12,marginBottom:24}}>
    <Icon name="history" style={{color:'rgba(233,193,118,0.4)'}}/>
    <h3 className="font-headline" style={{fontSize:22}}>Recently Consulted</h3>
  </div>
  {[
    {name:'Obsidian Scrying Meditation',cat:'Spells \u2022 Divination',time:'2 hours ago'},
    {name:'Properties of Blue Lace Agate',cat:'Crystals \u2022 Communication',time:'Yesterday'},
    {name:'The Lunar Gardening Almanac',cat:'Guides \u2022 Nature',time:'3 days ago'},
  ].map((e,i)=>(
    <div key={i} style={{padding:'20px 0',display:'flex',justifyContent:'space-between',alignItems:'center',
      cursor:'pointer',transition:'background .3s',borderRadius:2}}
      onMouseOver={ev=>ev.currentTarget.style.background='var(--surface-high)'}
      onMouseOut={ev=>ev.currentTarget.style.background='transparent'}>
      <div style={{display:'flex',alignItems:'center',gap:20,paddingLeft:8}}>
        <span className="font-label" style={{fontSize:10,color:'rgba(233,193,118,0.4)',width:24}}>{String(i+1).padStart(2,'0')}</span>
        <div>
          <h5 className="font-headline" style={{fontSize:16,marginBottom:4}}>{e.name}</h5>
          <p className="font-label" style={{fontSize:10,textTransform:'uppercase',letterSpacing:'0.1em',color:'rgba(227,225,234,0.4)'}}>{e.cat}</p>
        </div>
      </div>
      <div style={{display:'flex',alignItems:'center',gap:12,paddingRight:8}}>
        <span style={{fontSize:12,color:'rgba(227,225,234,0.3)',fontStyle:'italic'}}>{e.time}</span>
        <Icon name="arrow_forward_ios" style={{fontSize:16,color:'rgba(227,225,234,0.2)'}}/>
      </div>
    </div>
  ))}
</section>
```

  </div>
);

// ─── RITUALS PAGE ───
const RitualsPage = () => {
const [openStep, setOpenStep] = useState(0);
const steps = [
{num:‘01’,title:‘Sanctify the Perimeter’,
text:‘Begin by lighting your incense or sage bundle. Walk clockwise around your chosen space, allowing the smoke to reach every corner. Visualize a barrier of shimmering silver light forming at the boundaries.’,
img:IMG.sage},
{num:‘02’,title:‘The Inscription of Intent’,
text:‘Sit in the center of your space. On your parchment, write down three habits, thoughts, or situations you wish to release. Be specific and honest. Your words are the fuel for the lunar fire.’},
{num:‘03’,title:‘Lunar Exposure’,
text:‘Place your crystals and the spring water on a surface where they can catch the moonlight. Hold your selenite wand over your parchment and speak your intention aloud: “What no longer serves, I now release.”’},
{num:‘04’,title:‘Elemental Return’,
text:‘Carefully burn the parchment (using a fire-safe bowl) and scatter the ashes outside. Wash your hands with the moon-charged spring water to symbolize the completion of the cleansing.’},
];

return (
<div className="page-enter" style={{paddingTop:72,paddingBottom:100}}>
{/* Hero */}
<section style={{position:‘relative’,height:380,marginBottom:48,overflow:‘hidden’}}>
<img src={IMG.moon} alt=”” style={{width:‘100%’,height:‘100%’,objectFit:‘cover’,transition:‘transform .7s’}}/>
<div style={{position:‘absolute’,inset:0,background:‘linear-gradient(to top,var(–bg) 10%,rgba(18,19,25,0.2) 50%,transparent)’}}/>
<div style={{position:‘absolute’,bottom:32,left:24,right:24}}>
<div style={{display:‘flex’,gap:8,marginBottom:12}}>
<span className=“font-label” style={{fontSize:10,textTransform:‘uppercase’,letterSpacing:‘0.05em’,color:‘var(–tertiary)’,
background:‘rgba(30,19,0,0.3)’,padding:‘4px 12px’,borderRadius:2}}>Celestial Event</span>
<span className=“font-label” style={{fontSize:10,textTransform:‘uppercase’,letterSpacing:‘0.05em’,color:‘rgba(227,225,234,0.6)’}}>45 Min</span>
</div>
<h2 className=“font-headline animate-fadeUp” style={{fontSize:‘clamp(36px,8vw,56px)’,fontWeight:700,letterSpacing:’-0.02em’,marginBottom:12}}>Full Moon Cleansing</h2>
<p style={{fontSize:16,color:‘rgba(199,197,204,1)’,maxWidth:500,lineHeight:1.6}}>
A transformative ritual designed to release stagnant energies and reset your spiritual intention under the lunar peak.
</p>
</div>
</section>

```
  <div style={{padding:'0 24px'}}>
    {/* Essential Elements */}
    <section className="animate-fadeUp delay-1" style={{background:'var(--surface-low)',padding:28,borderRadius:4,marginBottom:32}}>
      <h3 className="font-headline" style={{fontSize:20,marginBottom:24,display:'flex',alignItems:'center',gap:8}}>
        <Icon name="inventory_2" style={{color:'var(--tertiary)'}}/>
        Essential Elements
      </h3>
      {[
        {icon:'diamond',name:'Selenite Wand',use:'For lunar conductivity'},
        {icon:'air',name:'White Sage or Palo Santo',use:'Atmospheric purification'},
        {icon:'water_drop',name:'Spring Water',use:'Elemental grounding'},
        {icon:'edit_note',name:'Parchment & Charcoal',use:'Sigil manifestation'},
      ].map((el,i)=>(
        <div key={i} style={{display:'flex',alignItems:'flex-start',gap:16,marginBottom:20}}>
          <div style={{width:40,height:40,borderRadius:4,background:'var(--surface-highest)',display:'flex',alignItems:'center',justifyContent:'center',flexShrink:0}}>
            <Icon name={el.icon} style={{color:'var(--on-surface-variant)',fontSize:20}}/>
          </div>
          <div>
            <p style={{fontWeight:600,marginBottom:2}}>{el.name}</p>
            <p style={{fontSize:11,textTransform:'uppercase',letterSpacing:'0.08em',color:'rgba(227,225,234,0.5)'}}>{el.use}</p>
          </div>
        </div>
      ))}
      <button style={{
        width:'100%',marginTop:16,background:'var(--tertiary)',color:'var(--on-tertiary)',border:'none',
        padding:'16px 0',cursor:'pointer',fontWeight:700,textTransform:'uppercase',letterSpacing:'0.1em',fontSize:13,
        fontFamily:'Manrope',boxShadow:'0 0 15px rgba(233,193,118,0.2)',transition:'transform .2s'
      }}
      onMouseOver={e=>e.target.style.transform='scale(1.02)'}
      onMouseOut={e=>e.target.style.transform='scale(1)'}>
        Prepare My Space
      </button>
    </section>

    {/* Alchemist's Note */}
    <div className="animate-fadeUp delay-2" style={{padding:28,borderRadius:4,background:'rgba(30,19,0,0.1)',marginBottom:40,position:'relative',overflow:'hidden'}}>
      <h4 className="font-headline" style={{fontStyle:'italic',color:'var(--tertiary)',marginBottom:8,fontSize:18}}>Alchemist's Note</h4>
      <p style={{fontSize:14,color:'var(--on-surface-variant)',lineHeight:1.7}}>
        The full moon's energy is strongest 24 hours before and after the astronomical peak. Time your ritual accordingly for maximum potency.
      </p>
    </div>

    {/* Rite of Release Steps */}
    <div style={{marginBottom:16}}>
      <h3 className="font-headline" style={{fontSize:28,marginBottom:8}}>The Rite of Release</h3>
      <p style={{color:'var(--on-surface-variant)',marginBottom:24}}>Follow these steps with focused intention and steady breath.</p>
    </div>
    {steps.map((s,i)=>(
      <div key={i} className={`animate-fadeUp delay-${Math.min(i+2,5)}`}
        style={{background:openStep===i?'var(--surface-high)':'var(--surface-container)',
          padding:20,borderRadius:4,marginBottom:8,transition:'background .3s',cursor:'pointer'}}
        onClick={()=>setOpenStep(openStep===i?-1:i)}>
        <div style={{display:'flex',justifyContent:'space-between',alignItems:'center'}}>
          <div style={{display:'flex',alignItems:'center',gap:20}}>
            <span className="font-headline" style={{fontSize:32,color:openStep===i?'var(--tertiary)':'var(--outline-variant)',transition:'color .3s'}}>{s.num}</span>
            <h4 style={{fontSize:18,fontWeight:600}}>{s.title}</h4>
          </div>
          <Icon name="expand_more" style={{transition:'transform .3s',transform:openStep===i?'rotate(180deg)':'rotate(0)'}}/>
        </div>
        {openStep===i && (
          <div className="animate-fadeIn" style={{marginTop:20,paddingLeft:52,borderLeft:'1px solid rgba(70,70,76,0.3)',marginLeft:16}}>
            <p style={{color:'var(--on-surface-variant)',lineHeight:1.7,marginBottom:s.img?16:0}}>{s.text}</p>
            {s.img && <img src={s.img} alt="" style={{width:'100%',height:160,objectFit:'cover',borderRadius:4,filter:'grayscale(0.6)',transition:'filter .5s'}}
              onMouseOver={e=>e.target.style.filter='grayscale(0)'}
              onMouseOut={e=>e.target.style.filter='grayscale(0.6)'}/>}
          </div>
        )}
      </div>
    ))}
  </div>
</div>
```

);
};

// ─── CORRESPONDENCE PAGE ───
const CorrespondencePage = () => {
const [filter, setFilter] = useState(‘all’);
const [search, setSearch] = useState(’’);
const filters = [‘all’,‘crystals’,‘herbs’,‘intentions’];
const items = [
{name:‘Amethyst’,cat:‘crystals’,tag:‘Crystal of the Moon’,desc:’“Peace, Intuition, and the expansion of the inner eye.”’,img:IMG.amethyst,big:true},
{name:‘Lavender’,cat:‘herbs’,tag:‘Purification \u2022 Tranquility’,desc:‘Used in alchemical baths to silence the noise of the mundane world.’,img:IMG.lavender,medium:true},
{name:‘Mugwort’,cat:‘herbs’,tag:‘Lucid Dreaming’,desc:‘Bridge the gap between waking and sleep.’,img:IMG.mugwort},
{name:‘Citrine’,cat:‘crystals’,tag:‘Abundance’,desc:‘The solar stone of manifestation and will.’,img:IMG.citrine},
{name:‘Rosemary’,cat:‘herbs’,tag:‘Remembrance’,desc:‘A mental tonic for clarity and sharp recall.’,img:IMG.rosemary},
{name:‘Moonstone’,cat:‘crystals’,tag:‘Cycles’,desc:‘Attunement to the rhythms of the celestial body.’,img:IMG.moonstone},
];
const filtered = items.filter(it=>{
const matchCat = filter===‘all’ || it.cat===filter || (filter===‘intentions’);
const matchSearch = !search || it.name.toLowerCase().includes(search.toLowerCase()) || it.desc.toLowerCase().includes(search.toLowerCase());
return matchCat && matchSearch;
});

return (
<div className=“page-enter” style={{paddingTop:72,paddingBottom:100,padding:‘72px 24px 100px’}}>
{/* Header */}
<h2 className=“font-headline animate-fadeUp” style={{fontSize:‘clamp(40px,8vw,56px)’,fontWeight:700,letterSpacing:’-0.02em’,marginBottom:12,lineHeight:1.05}}>
Correspondence <br/><em style={{color:‘var(–tertiary)’}}>Index</em>
</h2>
<p className=“animate-fadeUp delay-1” style={{color:‘var(–on-surface-variant)’,fontSize:16,marginBottom:32,maxWidth:500,lineHeight:1.6}}>
A curated database of alchemical properties, linking the celestial to the terrestrial. Seek the resonance of herb and stone.
</p>

```
  {/* Search */}
  <div className="animate-fadeUp delay-2" style={{position:'relative',marginBottom:32}}>
    <input type="text" value={search} onChange={e=>setSearch(e.target.value)}
      placeholder="Search crystals, herbs, or intent..."
      style={{width:'100%',background:'transparent',border:'none',borderBottom:'1px solid var(--outline-variant)',
        padding:'16px 40px 16px 0',color:'var(--on-surface)',fontFamily:'Manrope',fontSize:18,outline:'none'}}
      onFocus={e=>e.target.style.borderBottomColor='var(--tertiary)'}
      onBlur={e=>e.target.style.borderBottomColor='var(--outline-variant)'}/>
    <Icon name="auto_awesome" style={{position:'absolute',right:0,top:16,color:'var(--tertiary)'}}/>
  </div>

  {/* Filter Tabs */}
  <div className="animate-fadeUp delay-3" style={{display:'flex',gap:24,marginBottom:32,borderBottom:'1px solid rgba(70,70,76,0.15)',overflowX:'auto'}}>
    {filters.map(f=>(
      <button key={f} onClick={()=>setFilter(f)} className="font-label" style={{
        background:'none',border:'none',cursor:'pointer',paddingBottom:16,fontSize:13,
        textTransform:'uppercase',letterSpacing:'0.1em',transition:'all .3s',whiteSpace:'nowrap',
        color:filter===f?'var(--tertiary)':'rgba(227,225,234,0.5)',
        borderBottom:filter===f?'2px solid var(--tertiary)':'2px solid transparent',
      }}>{f==='all'?'All Elements':f.charAt(0).toUpperCase()+f.slice(1)}</button>
    ))}
  </div>

  {/* Items Grid */}
  <div style={{display:'flex',flexDirection:'column',gap:16}}>
    {filtered.map((it,i)=>(
      <div key={it.name} className={`animate-fadeUp delay-${Math.min(i+1,5)}`}
        style={it.big?{position:'relative',height:280,overflow:'hidden',borderRadius:4,background:'var(--surface-low)'}
          :it.medium?{display:'flex',flexDirection:'column',overflow:'hidden',borderRadius:4,background:'var(--surface-container)'}
          :{background:'var(--surface-low)',padding:20,borderRadius:4,border:'1px solid rgba(70,70,76,0.1)',transition:'background .3s'}}>
        {it.big ? <>
          <img src={it.img} alt="" style={{position:'absolute',inset:0,width:'100%',height:'100%',objectFit:'cover',opacity:0.6,transition:'transform .7s'}}/>
          <div style={{position:'absolute',inset:0,background:'linear-gradient(to top,var(--bg),rgba(18,19,25,0.2) 40%,transparent)'}}/>
          <div style={{position:'absolute',bottom:0,left:0,padding:24}}>
            <span className="font-label" style={{fontSize:10,textTransform:'uppercase',letterSpacing:'0.1em',color:'var(--tertiary)',
              background:'rgba(233,193,118,0.1)',border:'1px solid rgba(233,193,118,0.2)',padding:'4px 12px',display:'inline-block',marginBottom:12}}>{it.tag}</span>
            <h3 className="font-headline" style={{fontSize:32,fontWeight:700,marginBottom:8}}>{it.name}</h3>
            <p style={{color:'var(--on-surface-variant)',fontStyle:'italic',maxWidth:400}}>{it.desc}</p>
          </div>
        </> : it.medium ? <>
          <div style={{height:160,overflow:'hidden'}}>
            <img src={it.img} alt="" style={{width:'100%',height:'100%',objectFit:'cover',opacity:0.7,transition:'transform .5s'}}/>
          </div>
          <div style={{padding:20}}>
            <h3 className="font-headline" style={{fontSize:20,fontWeight:700,marginBottom:8}}>{it.name}</h3>
            <p className="font-label" style={{fontSize:10,textTransform:'uppercase',letterSpacing:'0.05em',color:'var(--on-surface-variant)',marginBottom:8}}>{it.tag}</p>
            <p style={{fontSize:14,color:'rgba(227,225,234,0.7)'}}>{it.desc}</p>
          </div>
        </> : <>
          <div style={{width:'100%',aspectRatio:'1',borderRadius:4,overflow:'hidden',marginBottom:16,background:'var(--surface-container)'}}>
            <img src={it.img} alt="" style={{width:'100%',height:'100%',objectFit:'cover',opacity:0.6,transition:'transform .5s'}}/>
          </div>
          <h4 className="font-headline" style={{fontSize:18,fontWeight:700,marginBottom:4}}>{it.name}</h4>
          <p className="font-label" style={{fontSize:10,textTransform:'uppercase',letterSpacing:'0.1em',color:'var(--tertiary)',fontStyle:'italic',marginBottom:8}}>{it.tag}</p>
          <p style={{fontSize:13,color:'var(--on-surface-variant)'}}>{it.desc}</p>
        </>}
      </div>
    ))}
  </div>

  {/* Weekly Correspondence */}
  <div className="animate-fadeUp" style={{marginTop:32,background:'rgba(52,54,59,0.4)',backdropFilter:'blur(24px)',
    borderRadius:4,padding:32,border:'1px solid rgba(70,70,76,0.15)'}}>
    <div style={{display:'flex',justifyContent:'space-between',alignItems:'flex-start',marginBottom:24}}>
      <h3 className="font-headline" style={{fontSize:22,fontWeight:700}}>Weekly<br/>Correspondence</h3>
      <Icon name="star" className="filled" style={{color:'var(--tertiary)'}}/>
    </div>
    <p style={{color:'var(--on-surface-variant)',fontSize:14,fontStyle:'italic',marginBottom:8}}>Current Astral Alignment:</p>
    <p className="font-headline" style={{fontSize:20,marginBottom:16}}>Sage & Lapis Lazuli</p>
    <div style={{height:1,background:'rgba(70,70,76,0.2)',marginBottom:16}}/>
    <p style={{fontSize:13,color:'rgba(199,197,204,0.7)',lineHeight:1.7}}>
      During this lunar phase, the combination of Sage and Lapis Lazuli enhances visionary work and linguistic precision.
    </p>
  </div>
</div>
```

);
};

// ─── GUIDES PAGE (Spell Casting) ───
const GuidesPage = () => {
const [seconds, setSeconds] = useState(671); // 11:11
const [running, setRunning] = useState(false);
const [checked, setChecked] = useState({1:true});
const intervalRef = useRef(null);
const totalTime = 671;

useEffect(()=>{
if(running && seconds > 0){
intervalRef.current = setInterval(()=>setSeconds(s=>s-1),1000);
} else {
clearInterval(intervalRef.current);
if(seconds<=0) setRunning(false);
}
return ()=>clearInterval(intervalRef.current);
},[running,seconds]);

const mins = Math.floor(seconds/60);
const secs = seconds%60;
const progress = ((totalTime - seconds)/totalTime)*283;

const essentials = [
{name:‘Silver Candle’,note:‘Charged under the full moon’},
{name:‘Obsidian Shard’,note:‘To anchor the intent’},
{name:‘Dried Sage’,note:‘For energetic purification’},
{name:‘Pure Water Source’,note:‘Ideally from a moving stream’},
];

return (
<div className=“page-enter” style={{paddingTop:72,paddingBottom:100,padding:‘72px 24px 100px’}}>
{/* Header */}
<div style={{textAlign:‘center’,marginBottom:40}}>
<span className=“font-label” style={{fontSize:10,textTransform:‘uppercase’,letterSpacing:‘0.2em’,color:‘var(–tertiary)’,
border:‘1px solid rgba(70,70,76,0.15)’,padding:‘6px 16px’,display:‘inline-block’,marginBottom:20}}>Active Working</span>
<h2 className=“font-headline animate-fadeUp” style={{fontSize:‘clamp(36px,8vw,52px)’,letterSpacing:’-0.02em’,marginBottom:20}}>
Celestial Alignment Ritual
</h2>
<p className=“animate-fadeUp delay-1” style={{color:‘rgba(227,225,234,0.8)’,fontSize:16,fontStyle:‘italic’,lineHeight:1.6,maxWidth:500,margin:‘0 auto’}}>
To harness the shifting tides of the lunar cycle and manifest clarity within the digital noise.
</p>
</div>

```
  {/* Moon Phase badge */}
  <div className="animate-fadeUp delay-2" style={{display:'flex',alignItems:'center',gap:16,background:'var(--surface-low)',padding:16,borderRadius:4,marginBottom:32}}>
    <div style={{width:48,height:48,borderRadius:4,background:'var(--surface-highest)',display:'flex',alignItems:'center',justifyContent:'center'}}>
      <Icon name="nights_stay" className="filled" style={{color:'var(--tertiary)'}}/>
    </div>
    <div>
      <div className="font-label" style={{fontSize:10,textTransform:'uppercase',color:'var(--on-surface-variant)',letterSpacing:'0.08em'}}>Current Phase</div>
      <div style={{fontWeight:500}}>Waxing Gibbous</div>
    </div>
  </div>

  {/* Casting Essentials */}
  <section className="animate-fadeUp delay-3" style={{background:'var(--surface-low)',padding:28,borderRadius:4,marginBottom:32}}>
    <h3 className="font-headline" style={{fontSize:20,marginBottom:24,display:'flex',alignItems:'center',gap:8}}>
      <span style={{width:6,height:6,background:'var(--tertiary)',display:'inline-block'}}/>
      Casting Essentials
    </h3>
    {essentials.map((e,i)=>(
      <div key={i} onClick={()=>setChecked(c=>({...c,[i]:!c[i]}))}
        style={{display:'flex',alignItems:'flex-start',gap:16,padding:8,margin:'-4px -8px',marginBottom:16,
          cursor:'pointer',borderRadius:4,transition:'background .3s'}}
        onMouseOver={ev=>ev.currentTarget.style.background='var(--surface-high)'}
        onMouseOut={ev=>ev.currentTarget.style.background='transparent'}>
        <div style={{width:20,height:20,border:`1px solid ${checked[i]?'var(--tertiary)':'var(--outline-variant)'}`,
          borderRadius:2,display:'flex',alignItems:'center',justifyContent:'center',flexShrink:0,marginTop:2,
          background:checked[i]?'rgba(233,193,118,0.1)':'var(--surface-lowest)',transition:'all .2s'}}>
          {checked[i] && <Icon name="check" style={{fontSize:14,color:'var(--tertiary)'}}/>}
        </div>
        <div style={{opacity:checked[i]?0.4:1,transition:'opacity .3s'}}>
          <span style={{fontWeight:500,display:'block',textDecoration:checked[i]?'line-through':'none'}}>{e.name}</span>
          <span style={{fontSize:12,color:'rgba(227,225,234,0.5)'}}>{e.note}</span>
        </div>
      </div>
    ))}
  </section>

  {/* Ritual Image */}
  <div className="animate-fadeUp delay-4" style={{height:200,borderRadius:4,overflow:'hidden',marginBottom:32,position:'relative',
    filter:'grayscale(0.6)',transition:'filter .7s',cursor:'crosshair'}}
    onMouseOver={e=>e.currentTarget.style.filter='grayscale(0)'}
    onMouseOut={e=>e.currentTarget.style.filter='grayscale(0.6)'}>
    <img src={IMG.altar} alt="" style={{width:'100%',height:'100%',objectFit:'cover',opacity:0.6}}/>
    <div style={{position:'absolute',inset:0,background:'linear-gradient(to top,var(--bg),transparent)'}}/>
    <span className="font-label" style={{position:'absolute',bottom:16,left:16,fontSize:10,textTransform:'uppercase',letterSpacing:'0.1em',color:'var(--tertiary)'}}>Visual Focus</span>
  </div>

  {/* Timer */}
  <div className="animate-fadeUp delay-5" style={{
    background:'var(--surface-low)',padding:40,borderRadius:4,marginBottom:32,
    display:'flex',flexDirection:'column',alignItems:'center',
    border:'1px solid rgba(70,70,76,0.05)',
    backgroundImage:'radial-gradient(circle,rgba(233,193,118,0.08) 0%,transparent 70%)'
  }}>
    <div style={{position:'relative',width:220,height:220,display:'flex',alignItems:'center',justifyContent:'center'}}>
      <svg style={{position:'absolute',width:'100%',height:'100%'}}>
        <circle cx="50%" cy="50%" r="45%" fill="none" stroke="rgba(70,70,76,0.2)" strokeWidth="2"/>
        <circle className="timer-ring" cx="50%" cy="50%" r="45%" fill="none"
          stroke="var(--tertiary)" strokeWidth="3" strokeLinecap="round"
          style={{strokeDashoffset:283-progress}}/>
      </svg>
      <div style={{textAlign:'center'}}>
        <div className="font-headline" style={{fontSize:44,letterSpacing:'0.1em'}}>{String(mins).padStart(2,'0')}:{String(secs).padStart(2,'0')}</div>
        <div className="font-label" style={{fontSize:10,textTransform:'uppercase',letterSpacing:'0.2em',color:'var(--tertiary)',marginTop:8}}>Ritual Focus</div>
      </div>
    </div>
    <div style={{display:'flex',gap:16,marginTop:32}}>
      <button onClick={()=>setRunning(!running)} className={running?'timer-active':''}
        style={{padding:'10px 28px',background:'var(--tertiary)',color:'var(--on-tertiary)',border:'none',
          cursor:'pointer',fontFamily:'Manrope',fontSize:11,textTransform:'uppercase',letterSpacing:'0.1em',fontWeight:600,
          transition:'all .3s'}}>
        {running?'Pause':'Begin'}
      </button>
      <button onClick={()=>{setRunning(false);setSeconds(totalTime)}}
        style={{padding:'10px 28px',background:'transparent',border:'1px solid rgba(70,70,76,0.15)',color:'var(--on-surface)',
          cursor:'pointer',fontFamily:'Manrope',fontSize:11,textTransform:'uppercase',letterSpacing:'0.1em',
          transition:'all .3s'}}>
        Reset
      </button>
    </div>
  </div>

  {/* Guidance */}
  <div style={{background:'var(--surface-lowest)',padding:28,border:'1px solid rgba(70,70,76,0.1)',borderRadius:4,marginBottom:32}}>
    <h4 className="font-headline" style={{fontSize:18,marginBottom:12}}>The Weaver's Breath</h4>
    <p style={{color:'var(--on-surface-variant)',fontSize:14,lineHeight:1.7}}>
      As the timer counts, focus on the rhythmic expansion of your lungs. Imagine the breath as a silken thread, weaving through the objects on your altar, binding your intention to the physical world.
    </p>
    <div style={{marginTop:20,display:'flex',alignItems:'center',gap:12,color:'rgba(233,193,118,0.6)'}}>
      <Icon name="auto_fix_high"/>
      <span className="font-label" style={{fontSize:10,textTransform:'uppercase',letterSpacing:'0.1em'}}>Active Intention Active</span>
    </div>
  </div>

  {/* Incantation */}
  <div style={{background:'var(--surface-low)',padding:32,borderRadius:4,borderTop:'4px solid rgba(233,193,118,0.2)',textAlign:'center'}}>
    <Icon name="history_edu" style={{color:'var(--tertiary)',fontSize:36,display:'block',margin:'0 auto 16'}}/>
    <h3 className="font-headline" style={{fontSize:22,marginBottom:8}}>The Spoken Incantation</h3>
    <p className="font-label" style={{fontSize:10,textTransform:'uppercase',letterSpacing:'0.1em',color:'var(--on-surface-variant)',marginBottom:28}}>Recite with intentionality</p>
    <p className="font-headline" style={{fontSize:'clamp(20px,5vw,28px)',lineHeight:1.5}}>
      "By moon's bright face and shadow's deep,<br/>
      Awaken what within me sleep.<br/>
      From digital noise to sacred light,<br/>
      I claim my voice within the night."
    </p>
    <div style={{marginTop:28,paddingTop:24,borderTop:'1px solid rgba(70,70,76,0.1)'}}>
      <p style={{fontSize:14,color:'rgba(227,225,234,0.6)',fontStyle:'italic',marginBottom:20}}>
        Repeat three times, allowing the final syllable of the third recitation to resonate until the timer concludes.
      </p>
      <div style={{display:'flex',flexWrap:'wrap',justifyContent:'center',gap:8}}>
        {['Phase 1: Whisper','Phase 2: Resonance','Phase 3: Conviction'].map(p=>(
          <span key={p} style={{padding:'6px 16px',background:'var(--surface-highest)',fontSize:9,fontFamily:'Manrope',
            textTransform:'uppercase',letterSpacing:'0.1em',borderRadius:20}}>{p}</span>
        ))}
      </div>
    </div>
  </div>
</div>
```

);
};

// ─── APP ───
const App = () => {
const [page, setPage] = useState(‘library’);
const navigate = useCallback((p)=>{setPage(p);window.scrollTo(0,0);},[]);

const renderPage = () => {
switch(page){
case ‘library’: return <LibraryPage onNavigate={navigate}/>;
case ‘rituals’: return <RitualsPage/>;
case ‘correspondence’: return <CorrespondencePage/>;
case ‘guides’: return <GuidesPage/>;
default: return <LibraryPage onNavigate={navigate}/>;
}
};

return (
<>
{/* Background atmospheric elements */}
<div style={{position:‘fixed’,inset:0,pointerEvents:‘none’,zIndex:-1,overflow:‘hidden’}}>
<div style={{position:‘absolute’,top:‘25%’,left:-80,width:350,height:350,background:‘rgba(233,193,118,0.04)’,borderRadius:‘50%’,filter:‘blur(120px)’}}/>
<div style={{position:‘absolute’,bottom:‘25%’,right:-80,width:300,height:300,background:‘rgba(196,197,219,0.04)’,borderRadius:‘50%’,filter:‘blur(100px)’}}/>
</div>
<Header onSearch={()=>{}}/>
<main style={{maxWidth:768,margin:‘0 auto’}}>
{renderPage()}
</main>
<BottomNav active={page} onChange={navigate}/>
</>
);
};

ReactDOM.createRoot(document.getElementById(‘root’)).render(<App/>);
</script>

</body>
</html>
