import { useState, useEffect, useRef, useCallback } from “react”;

const IMG = {
hero: “https://lh3.googleusercontent.com/aida-public/AB6AXuDche_LNDv0fnckdDdbCUo2oLIsrNeAwDiaUXlziFPIwYNEm4O91FqqWrJfZLW9A9vmvMOsduRdUSmHbxTVx9yHdtcPYfRjqeRS9vfySbIInFPfYwoBoucMkNI28yKEsCm14b1sdToznenCnHsOnRO0uKzP56DY1YMbILpzGMXky1IOlDrUiYW5iIfBrR3_rKjpnJLje43xRumeUXf7TXElvMPfDUQWmT38TUAZsZHm1vgAQkDjwR_3cvMN2d_DkrzPS5x2qG8EnEQ”,
spells: “https://lh3.googleusercontent.com/aida-public/AB6AXuBquZ3mWXh0FgZQnLueE8VkpiEvhbei-JZbN7vNi0TCxWc7PfshQgaAX4bal67uZlvsnYOVGPt8dg9giEEmod_p9jmylzxz4l4SUFMrDwKb65w9xtMeG9GCm4m-w8yvbzGwwPMv3VMC0H05zBEmigIKlT_T73t2Ppm0jbpkxrUMZMdNeqyYq-5TvaqkW9SRZK0OgvBDuQ7tvBuhGFfP–gDrUUDVnu9KdOMGVQa0Py8_b0qgGWTjDM0ctYCrWva8Ylw9oJzCCCOWkc”,
crystals: “https://lh3.googleusercontent.com/aida-public/AB6AXuBisE9bKoAM7NhI2b6HtLVQJqWemNBNlutmM45ICe-83R7xNd5XSO4sDRCo9uPMVmkDtf7VNoDCk62icabPUSJk756EICBgsKI2nSRj66vDWokKdHDG702NMuZmuNLVcXgWaJknK0ymv66C3DS74VJ6P8_1UPZ_nO5I6DgVEN268wd7csAqpaqdDFtVvOqa8KGloUHJS9YvK1kf8H11FpS-mmg4_PLuQ-edTtXlmcQ6mtdKh1EdjXa6Cyk337Mp1V4hNXhSuNv-nMM”,
herbs: “https://lh3.googleusercontent.com/aida-public/AB6AXuCtUGxaNwDCnPbO4kLgzuPuQcyHDXjvVLHx0N-wx_qQHz04i6neHuP0JZvOjgvfMVGq0jekhG6nO7ld6R4JW_EyZkla3JXLYDoI3atI4R9_FUpbKmLU2V9dO4pXIGeK29yYetNsR7QzfF_ulhwd_BpFQRimJIeurWb_EA8ZW1lnM2GHZZ5mR5n-LfAYtQS55qRdB2NWE_CkquT-RvPCG5p-wvi17dcYL7mp-ept6MFsK_BNXzgRDywN97lYnxysiDXdNFiaynTVtPQ”,
amethyst: “https://lh3.googleusercontent.com/aida-public/AB6AXuDc0MDo8bg1zsrUQ9EWO85RSRlEIH4IQxsknv5k2Ujb3pSSrEoRM4w7ftJU3nQFKst3rf8TQ3bsE3yBGmiTWpdvT5QbB_hVxYAUXoWh0PCW5hoWOLAk-EKrbrXFaY2Z7A9Wzki8KSKgFyxPQjTb7iwpMNpcQsjyUygE7VumJFnxAIYzCKmKWMmqyYEtXISAgi76WiAgCpNKYx8AZbOnoy0SHlts6J5kd0T_qtsubxVlQYTQRXx5piVOT6v5ot1a6sv0QBCOnoBmLlk”,
lavender: “https://lh3.googleusercontent.com/aida-public/AB6AXuCs_WJP6qr-N1EWHW90tTB5ZDVlbZjX22l2EySxnDTOeo64SVAqxdS2v0Pwzcyv3gL3uN6sl05NuHZey_ZBV4nBMvlwKqWS2SxyDczV_XnHDLWIf27lgiVcssFdNkz8LCzU_EKy-gzAgQgVurOM06-LVVfJsgYNvIjyHAl5RieYy19UqpQpfRD0b4NnzL95GVQHVBikHeUCsvGWara8m9_U0Kjl7zz5NDx4D-dwzNYNfNwrxYH5PnZm9hGGOl5NxU28FTWdwCN_ZZU”,
mugwort: “https://lh3.googleusercontent.com/aida-public/AB6AXuB02CCnITSxrM7cd1auZjfkj7lcKOmQLFegPe-i13YU-pMcDT8F3LmfKv4pQ8RJayDM_W1kuD5PKTA6P-vhSxPOOBE5NqfdkYd0dV6MTXwPhRtYLZjw0_AE7acqU07eBA1OWqvIaZ8LkucIrsEchjrxwa3Lx-iw9YXJo0GiG6SrCnDI3s06cGKIEdlKy1gKpeaGXegAxchoYVFr26xiCL82vnlWj_FPzf9MdRVqLtCGvzRTJpoU7I_WWB1VwGqon4m5r1_TybaSWFE”,
citrine: “https://lh3.googleusercontent.com/aida-public/AB6AXuAZbTZLvSDlYBNZaUhbSEnrulW7-sGLn24CNlVwq5Ms7kdDNtY3ktbt5QBpxV-3TKs9hb2l7lq61reMQkUPNOIrtuPyGoIvte_9pSW8OOQi4MD9NMRWRqIaYvCYWiTHNEYbrdpbFDnKDvatb4FTZi070-4mTyHmQoTZ1SVbPug_w-nkKfPD2S9CN7z1I6SeswcwYKjZANlGsUy88mkIkOdy-hodeG5exHszYqHHfr12RvQ1CBLVr9Yf7EFkAy3aTCXTR4wy9gPluMg”,
rosemary: “https://lh3.googleusercontent.com/aida-public/AB6AXuDokfcGXC4qL86v4IXKAVY3IT2TbO8cxjqdILevWd8ZIhQYJlK6YmnbDojwtiL3dDCSa4yEpuib9RWn295nRfp_mFCcmHxNnNEk99xsJs4NukJYQKO3yfMrpcL1WDNbehSkR6qzVddorCx9Bo4LDQnz9Dq9QmAgJk85JvKiXRayT1f-2qWvcY0J88663eOmyD4CBnsfFDSZJ5_42Yhiszrzcd7XRaloKPy_88QSBAsZtXhJuk-hGjJnngEOpm82_UId8XAEgEM9fa8”,
moonstone: “https://lh3.googleusercontent.com/aida-public/AB6AXuC-PPqmsO9aAr704z8sN-bYQECjUyyG6djUHD-NrBTnHfoVGPtHcWbi5HQMqNZQ_-wb5koDtm-gI2OQWWLswvGfd_GpMRbmW_AkTxJji1EDkoRhYXyiRv7K-zqFJBi0uTZ3bp9FYhySjw9sPNGLQ-lcCd2HXnwVw3mVWP0EBGpJsioTh7603H7ap4B4jmoQa8GOC9rGaF8GiW9Nbj5VtYBZ-qfrMTHebiDPmB7KOpxtqppQigxpuGl46bATJpaFuRTN3aD8MOKFxFc”,
moon: “https://lh3.googleusercontent.com/aida-public/AB6AXuCmKESE_VWMBmmhjTwhKa8v9ur6AxwbvzeIoZW0XssKlFZQlBXs-JYvUM7JcVaZkSqUg5ByrLGls-uflr3fk1w8DbaOZEdaxGu4VyhK0-defRUwl6d9nGf4efKmEYpnUQ85Sc2gjWq6bx-OOdfqniru2Zn2xUElALQOqR8COcExPoxKFbNabB1EnTxpTWgK0ElXoL59l90nqj04Jw_40ZOMILCmnDpd-nbPgbQ9ufMngZyA3evCfpqmH7K2c5KWdxXEPnbT3E9U8V8”,
sage: “https://lh3.googleusercontent.com/aida-public/AB6AXuCWOsXd2t2p2heJD65-ljW-CfvsD0Vpsw5OtVv4nQT8IztjebHWz3Y_J8I6ufJaVkpUMbyEsp30s68BWFE0Nt-6JQFRHlrceKWEXt27FfMaHrUqFEXTkJ1h6Uj8s7bGto6Re9erHuUcx6fjFo2tNkO1PiN3wwe8sUlKM9V69y_1SPBoXM5VhEjliPGzgrXVKd9xX8Uoik7qyS0FRZU_7hum1gRQVMKAsaTfyCOh3rRBLBoUo0bN-DPLKdr29a8WanGVRHT6ktlpXEw”,
altar: “https://lh3.googleusercontent.com/aida-public/AB6AXuC_sWgKnAfX2NhCQtt8Tv9Yw0OmHP4qLih5OLH4l1H6YIE_xKWVPksHpCkwO25JPnBPHLJliNniXifTKGTeb4TohkoRZfo3b62F58gWN8Fxv0j80S8sPxoVpIx6Toit9-fHqyMVvdpFs1opdpVARrdoeAfP9tpUBgTmKuoI3V9BsOeoN1fSNm0Bt0WmumiYFncAJ1hz8Ll6VMq8L5WhHC05e4glvjZtgneDgu4jH0t2MroOJpye000OJPEVqC2NBhWiTUQ-e4gycHg”,
};

// ─── Shared Styles ───
const colors = {
bg: “#121319”, surfaceLow: “#1a1b22”, surfaceContainer: “#1e1f26”,
surfaceHigh: “#292a30”, surfaceHighest: “#34343b”, surfaceBright: “#383940”,
surfaceLowest: “#0d0e14”, onSurface: “#e3e1ea”, onSurfaceVariant: “#c7c5cc”,
tertiary: “#e9c176”, onTertiary: “#412d00”, outlineVariant: “#46464c”,
tertiaryContainer: “#1e1300”, primary: “#c4c5db”,
};

// ─── Bottom Nav ───
const BottomNav = ({ active, onChange }) => {
const tabs = [
{ id: “library”, icon: “auto_stories”, label: “Library” },
{ id: “rituals”, icon: “auto_fix_high”, label: “Rituals” },
{ id: “correspondence”, icon: “menu_book”, label: “Index” },
{ id: “guides”, icon: “explore”, label: “Guides” },
];
return (
<nav className=“fixed bottom-0 left-0 right-0 z-50 flex justify-around items-center h-[72px]”
style={{ background: “rgba(18,19,25,0.92)”, backdropFilter: “blur(24px)”, borderTop: “1px solid rgba(70,70,76,0.15)”, boxShadow: “0 -8px 32px rgba(0,0,0,0.4)” }}>
{tabs.map((t) => (
<button key={t.id} onClick={() => onChange(t.id)}
className=“flex flex-col items-center gap-1 transition-all duration-300”
style={{ background: “none”, border: “none”, cursor: “pointer”,
color: active === t.id ? colors.tertiary : “rgba(227,225,234,0.5)”,
transform: active === t.id ? “scale(1.05)” : “scale(1)”,
filter: active === t.id ? “drop-shadow(0 0 8px rgba(233,193,118,0.3))” : “none” }}>
<span className="material-symbols-outlined">{t.icon}</span>
<span style={{ fontFamily: “Manrope”, fontSize: 10, textTransform: “uppercase”, letterSpacing: “0.05em” }}>{t.label}</span>
</button>
))}
</nav>
);
};

// ─── Header ───
const Header = () => (

  <header className="fixed top-0 left-0 right-0 z-50 flex justify-between items-center px-6 py-4"
    style={{ background: "linear-gradient(to bottom, #1a1b22, transparent)" }}>
    <div className="flex items-center gap-3">
      <span className="material-symbols-outlined" style={{ color: colors.tertiary, fontSize: 28, fontVariationSettings: "'FILL' 1" }}>menu_book</span>
      <h1 style={{ fontFamily: "'Noto Serif', serif", fontSize: 22, letterSpacing: "-0.02em", color: colors.onSurface }}>The Modern Grimoire</h1>
    </div>
    <button className="p-2 rounded transition-colors duration-300 hover:bg-[#34343b]" style={{ background: "none", border: "none", cursor: "pointer" }}>
      <span className="material-symbols-outlined" style={{ color: "rgba(227,225,234,0.6)" }}>search</span>
    </button>
  </header>
);

// ─── Label ───
const Label = ({ children, className = “” }) => (
<span className={className} style={{ fontFamily: “Manrope”, fontSize: 10, textTransform: “uppercase”, letterSpacing: “0.15em” }}>{children}</span>
);

// ─── LIBRARY PAGE ───
const LibraryPage = ({ onNavigate }) => (

  <div className="pt-20 pb-28 animate-[fadeIn_0.4s_ease-out]">
    <section className="px-6 mb-12">
      <div className="relative">
        <div className="absolute -top-20 -left-20 w-72 h-72 rounded-full pointer-events-none"
          style={{ background: "radial-gradient(circle,rgba(233,193,118,0.08) 0%,transparent 70%)" }} />
        <Label className="block mb-4" style={{ color: colors.tertiary }}>Current Focus</Label>
        <h2 style={{ fontFamily: "'Noto Serif', serif", fontSize: "clamp(38px,8vw,60px)", lineHeight: 1.05, marginBottom: 24, letterSpacing: "-0.02em", color: colors.onSurface }}>
          Ritual of the <br /><em style={{ color: "rgba(227,225,234,0.8)" }}>Celestial Equinox</em>
        </h2>
      </div>
      <div className="relative overflow-hidden" style={{ borderRadius: 2 }}>
        <img src={IMG.hero} alt="" className="w-full object-cover" style={{ height: 360, filter: "brightness(0.75) grayscale(0.2)" }} />
        <div className="absolute inset-0" style={{ background: "linear-gradient(to top,#121319,transparent 60%)" }} />
        <div className="absolute bottom-6 left-6 right-6">
          <p className="mb-5" style={{ color: "rgba(227,225,234,0.7)", lineHeight: 1.6, maxWidth: 380, fontSize: 14 }}>
            Aligning your inner frequency with the balance of the shifting seasons. Requires Lavender, Clear Quartz, and focus.
          </p>
          <button onClick={() => onNavigate("rituals")}
            style={{ background: colors.tertiary, color: colors.onTertiary, border: "none", padding: "12px 32px", cursor: "pointer",
              fontFamily: "Manrope", fontSize: 11, fontWeight: 600, textTransform: "uppercase", letterSpacing: "0.15em",
              boxShadow: "0 0 15px rgba(233,193,118,0.2)" }}>
            Begin Ritual
          </button>
        </div>
      </div>
    </section>

```
<section className="px-6 mb-12 flex flex-col gap-4">
  <div className="p-7" style={{ background: colors.surfaceLow, borderLeft: "2px solid rgba(233,193,118,0.2)" }}>
    <Label className="block mb-2" style={{ color: "rgba(233,193,118,0.6)" }}>Phase</Label>
    <h3 style={{ fontFamily: "'Noto Serif', serif", fontSize: 22, marginBottom: 10, color: colors.onSurface }}>Waxing Gibbous</h3>
    <p style={{ fontSize: 14, color: "rgba(227,225,234,0.5)", lineHeight: 1.6, fontStyle: "italic" }}>An auspicious time for growth, manifestation, and the gathering of power.</p>
  </div>
  <div className="p-7 relative overflow-hidden" style={{ background: colors.surfaceHighest }}>
    <Label className="block mb-3" style={{ color: colors.tertiary }}>Daily Insight</Label>
    <p style={{ fontFamily: "'Noto Serif', serif", fontSize: 18, fontStyle: "italic", lineHeight: 1.5, color: colors.onSurface }}>
      "The stars only reveal what the soul is ready to perceive."
    </p>
  </div>
</section>

<section className="px-6 mb-12">
  <div className="flex justify-between items-end mb-6">
    <h3 style={{ fontFamily: "'Noto Serif', serif", fontSize: 26, color: colors.onSurface }}>The Collections</h3>
    <Label style={{ color: "rgba(233,193,118,0.6)" }}>View All Wisdom</Label>
  </div>
  <div className="flex flex-col gap-4">
    {[
      { name: "Spells", img: IMG.spells, icon: "auto_fix_high", sub: "Incantations for the modern seeker. 142 entries.", h: 200 },
      { name: "Crystals", img: IMG.crystals, icon: "diamond", sub: "Vibrational conduits. 89 entries.", h: 170, grayscale: true },
      { name: "Herbs", img: IMG.herbs, icon: "eco", sub: "Botanical alchemy. 115 entries.", h: 170 },
    ].map((c) => (
      <div key={c.name} onClick={() => onNavigate("correspondence")}
        className="relative overflow-hidden cursor-pointer" style={{ height: c.h, background: colors.surfaceLow }}>
        <img src={c.img} alt="" className="absolute inset-0 w-full h-full object-cover"
          style={{ opacity: 0.4, filter: c.grayscale ? "grayscale(1)" : "none" }} />
        <div className="relative h-full p-6 flex flex-col justify-between">
          <div>
            <span className="material-symbols-outlined block mb-3" style={{ color: colors.tertiary, fontSize: 26 }}>{c.icon}</span>
            <h4 style={{ fontFamily: "'Noto Serif', serif", fontSize: c.h > 180 ? 30 : 24, color: colors.onSurface }}>{c.name}</h4>
          </div>
          <Label style={{ color: "rgba(227,225,234,0.6)" }}>{c.sub}</Label>
        </div>
      </div>
    ))}
    <div onClick={() => onNavigate("correspondence")} className="relative overflow-hidden cursor-pointer p-8"
      style={{ background: colors.surfaceLow }}>
      <div className="absolute inset-0 pointer-events-none"
        style={{ background: "radial-gradient(circle,rgba(233,193,118,0.08) 0%,transparent 70%)", opacity: 0.2 }} />
      <div className="relative">
        <span className="material-symbols-outlined block mb-3" style={{ color: colors.tertiary, fontSize: 26 }}>join_inner</span>
        <h4 style={{ fontFamily: "'Noto Serif', serif", fontSize: 30, marginBottom: 10, color: colors.onSurface }}>Correspondences</h4>
        <p style={{ color: "rgba(227,225,234,0.6)", lineHeight: 1.6, maxWidth: 380 }}>
          Map the intricate links between planets, days, colors, and intent to amplify your manifestations.
        </p>
      </div>
    </div>
  </div>
</section>

<section className="px-6">
  <div className="flex items-center gap-3 mb-6">
    <span className="material-symbols-outlined" style={{ color: "rgba(233,193,118,0.4)" }}>history</span>
    <h3 style={{ fontFamily: "'Noto Serif', serif", fontSize: 22, color: colors.onSurface }}>Recently Consulted</h3>
  </div>
  {[
    { name: "Obsidian Scrying Meditation", cat: "Spells \u2022 Divination", time: "2 hours ago" },
    { name: "Properties of Blue Lace Agate", cat: "Crystals \u2022 Communication", time: "Yesterday" },
    { name: "The Lunar Gardening Almanac", cat: "Guides \u2022 Nature", time: "3 days ago" },
  ].map((e, i) => (
    <div key={i} className="flex justify-between items-center py-5 px-2 cursor-pointer rounded transition-colors duration-300 hover:bg-[#292a30]">
      <div className="flex items-center gap-5">
        <span style={{ fontFamily: "Manrope", fontSize: 10, color: "rgba(233,193,118,0.4)", width: 24 }}>
          {String(i + 1).padStart(2, "0")}
        </span>
        <div>
          <h5 style={{ fontFamily: "'Noto Serif', serif", fontSize: 16, marginBottom: 4, color: colors.onSurface }}>{e.name}</h5>
          <Label style={{ color: "rgba(227,225,234,0.4)" }}>{e.cat}</Label>
        </div>
      </div>
      <div className="flex items-center gap-3">
        <span style={{ fontSize: 12, color: "rgba(227,225,234,0.3)", fontStyle: "italic" }}>{e.time}</span>
        <span className="material-symbols-outlined" style={{ fontSize: 16, color: "rgba(227,225,234,0.2)" }}>arrow_forward_ios</span>
      </div>
    </div>
  ))}
</section>
```

  </div>
);

// ─── RITUALS PAGE ───
const RitualsPage = () => {
const [openStep, setOpenStep] = useState(0);
const steps = [
{ num: “01”, title: “Sanctify the Perimeter”, text: “Begin by lighting your incense or sage bundle. Walk clockwise around your chosen space, allowing the smoke to reach every corner. Visualize a barrier of shimmering silver light forming at the boundaries.”, img: IMG.sage },
{ num: “02”, title: “The Inscription of Intent”, text: “Sit in the center of your space. On your parchment, write down three habits, thoughts, or situations you wish to release. Be specific and honest. Your words are the fuel for the lunar fire.” },
{ num: “03”, title: “Lunar Exposure”, text: ‘Place your crystals and the spring water on a surface where they can catch the moonlight. Hold your selenite wand over your parchment and speak your intention aloud: “What no longer serves, I now release.”’ },
{ num: “04”, title: “Elemental Return”, text: “Carefully burn the parchment (using a fire-safe bowl) and scatter the ashes outside. Wash your hands with the moon-charged spring water to symbolize the completion of the cleansing.” },
];

return (
<div className="pt-0 pb-28 animate-[fadeIn_0.4s_ease-out]">
<section className=“relative overflow-hidden” style={{ height: 380 }}>
<img src={IMG.moon} alt="" className="w-full h-full object-cover" />
<div className=“absolute inset-0” style={{ background: “linear-gradient(to top,#121319 10%,rgba(18,19,25,0.2) 50%,transparent)” }} />
<div className="absolute bottom-8 left-6 right-6">
<div className="flex gap-2 mb-3">
<span style={{ fontFamily: “Manrope”, fontSize: 10, textTransform: “uppercase”, letterSpacing: “0.05em”,
color: colors.tertiary, background: “rgba(30,19,0,0.3)”, padding: “4px 12px”, borderRadius: 2 }}>Celestial Event</span>
<span style={{ fontFamily: “Manrope”, fontSize: 10, textTransform: “uppercase”, letterSpacing: “0.05em”,
color: “rgba(227,225,234,0.6)”, padding: “4px 0” }}>45 Min</span>
</div>
<h2 style={{ fontFamily: “‘Noto Serif’, serif”, fontSize: “clamp(34px,8vw,52px)”, fontWeight: 700,
letterSpacing: “-0.02em”, marginBottom: 10, color: colors.onSurface }}>Full Moon Cleansing</h2>
<p style={{ fontSize: 15, color: colors.onSurfaceVariant, maxWidth: 460, lineHeight: 1.6 }}>
A transformative ritual designed to release stagnant energies and reset your spiritual intention under the lunar peak.
</p>
</div>
</section>

```
  <div className="px-6 pt-8">
    <div className="p-7 rounded mb-8" style={{ background: colors.surfaceLow }}>
      <h3 className="flex items-center gap-2 mb-6" style={{ fontFamily: "'Noto Serif', serif", fontSize: 20, color: colors.onSurface }}>
        <span className="material-symbols-outlined" style={{ color: colors.tertiary }}>inventory_2</span>
        Essential Elements
      </h3>
      {[
        { icon: "diamond", name: "Selenite Wand", use: "For lunar conductivity" },
        { icon: "air", name: "White Sage or Palo Santo", use: "Atmospheric purification" },
        { icon: "water_drop", name: "Spring Water", use: "Elemental grounding" },
        { icon: "edit_note", name: "Parchment & Charcoal", use: "Sigil manifestation" },
      ].map((el, i) => (
        <div key={i} className="flex items-start gap-4 mb-5">
          <div className="w-10 h-10 rounded flex items-center justify-center flex-shrink-0"
            style={{ background: colors.surfaceHighest }}>
            <span className="material-symbols-outlined" style={{ color: colors.onSurfaceVariant, fontSize: 20 }}>{el.icon}</span>
          </div>
          <div>
            <p style={{ fontWeight: 600, marginBottom: 2, color: colors.onSurface }}>{el.name}</p>
            <p style={{ fontSize: 11, textTransform: "uppercase", letterSpacing: "0.08em", color: "rgba(227,225,234,0.5)" }}>{el.use}</p>
          </div>
        </div>
      ))}
      <button className="w-full mt-4" style={{ background: colors.tertiary, color: colors.onTertiary, border: "none",
        padding: "16px 0", cursor: "pointer", fontFamily: "Manrope", fontWeight: 700, textTransform: "uppercase",
        letterSpacing: "0.1em", fontSize: 13, boxShadow: "0 0 15px rgba(233,193,118,0.2)" }}>
        Prepare My Space
      </button>
    </div>

    <div className="p-7 rounded mb-10 relative overflow-hidden" style={{ background: "rgba(30,19,0,0.1)" }}>
      <h4 style={{ fontFamily: "'Noto Serif', serif", fontStyle: "italic", color: colors.tertiary, marginBottom: 8, fontSize: 18 }}>
        Alchemist's Note
      </h4>
      <p style={{ fontSize: 14, color: colors.onSurfaceVariant, lineHeight: 1.7 }}>
        The full moon's energy is strongest 24 hours before and after the astronomical peak. Time your ritual accordingly for maximum potency.
      </p>
    </div>

    <h3 style={{ fontFamily: "'Noto Serif', serif", fontSize: 26, marginBottom: 6, color: colors.onSurface }}>The Rite of Release</h3>
    <p style={{ color: colors.onSurfaceVariant, marginBottom: 20 }}>Follow these steps with focused intention and steady breath.</p>

    <div className="flex flex-col gap-2">
      {steps.map((s, i) => (
        <div key={i} onClick={() => setOpenStep(openStep === i ? -1 : i)}
          className="p-5 rounded cursor-pointer transition-colors duration-300"
          style={{ background: openStep === i ? colors.surfaceHigh : colors.surfaceContainer }}>
          <div className="flex justify-between items-center">
            <div className="flex items-center gap-5">
              <span style={{ fontFamily: "'Noto Serif', serif", fontSize: 30,
                color: openStep === i ? colors.tertiary : colors.outlineVariant, transition: "color 0.3s" }}>{s.num}</span>
              <h4 style={{ fontSize: 17, fontWeight: 600, color: colors.onSurface }}>{s.title}</h4>
            </div>
            <span className="material-symbols-outlined transition-transform duration-300"
              style={{ transform: openStep === i ? "rotate(180deg)" : "rotate(0)", color: colors.onSurfaceVariant }}>expand_more</span>
          </div>
          {openStep === i && (
            <div className="mt-5 pl-12 ml-4 animate-[fadeIn_0.3s_ease-out]"
              style={{ borderLeft: "1px solid rgba(70,70,76,0.3)" }}>
              <p style={{ color: colors.onSurfaceVariant, lineHeight: 1.7, marginBottom: s.img ? 16 : 0 }}>{s.text}</p>
              {s.img && <img src={s.img} alt="" className="w-full rounded object-cover" style={{ height: 160, filter: "grayscale(0.6)" }} />}
            </div>
          )}
        </div>
      ))}
    </div>
  </div>
</div>
```

);
};

// ─── CORRESPONDENCE PAGE ───
const CorrespondencePage = () => {
const [filter, setFilter] = useState(“all”);
const [search, setSearch] = useState(””);
const [focused, setFocused] = useState(false);
const filters = [“all”, “crystals”, “herbs”, “intentions”];
const items = [
{ name: “Amethyst”, cat: “crystals”, tag: “Crystal of the Moon”, desc: “Peace, Intuition, and the expansion of the inner eye.”, img: IMG.amethyst, layout: “hero” },
{ name: “Lavender”, cat: “herbs”, tag: “Purification \u2022 Tranquility”, desc: “Used in alchemical baths to silence the noise of the mundane world.”, img: IMG.lavender, layout: “medium” },
{ name: “Mugwort”, cat: “herbs”, tag: “Lucid Dreaming”, desc: “Bridge the gap between waking and sleep.”, img: IMG.mugwort, layout: “card” },
{ name: “Citrine”, cat: “crystals”, tag: “Abundance”, desc: “The solar stone of manifestation and will.”, img: IMG.citrine, layout: “card” },
{ name: “Rosemary”, cat: “herbs”, tag: “Remembrance”, desc: “A mental tonic for clarity and sharp recall.”, img: IMG.rosemary, layout: “card” },
{ name: “Moonstone”, cat: “crystals”, tag: “Cycles”, desc: “Attunement to the rhythms of the celestial body.”, img: IMG.moonstone, layout: “card” },
];
const filtered = items.filter((it) => {
const matchCat = filter === “all” || it.cat === filter;
const matchSearch = !search || it.name.toLowerCase().includes(search.toLowerCase()) || it.desc.toLowerCase().includes(search.toLowerCase());
return matchCat && matchSearch;
});

return (
<div className="pt-20 pb-28 px-6 animate-[fadeIn_0.4s_ease-out]">
<h2 style={{ fontFamily: “‘Noto Serif’, serif”, fontSize: “clamp(38px,8vw,52px)”, fontWeight: 700,
letterSpacing: “-0.02em”, marginBottom: 10, lineHeight: 1.05, color: colors.onSurface }}>
Correspondence <br /><em style={{ color: colors.tertiary }}>Index</em>
</h2>
<p style={{ color: colors.onSurfaceVariant, fontSize: 15, marginBottom: 28, maxWidth: 460, lineHeight: 1.6 }}>
A curated database of alchemical properties, linking the celestial to the terrestrial. Seek the resonance of herb and stone.
</p>

```
  <div className="relative mb-8">
    <input type="text" value={search} onChange={(e) => setSearch(e.target.value)}
      onFocus={() => setFocused(true)} onBlur={() => setFocused(false)}
      placeholder="Search crystals, herbs, or intent..."
      className="w-full bg-transparent outline-none"
      style={{ border: "none", borderBottom: `1px solid ${focused ? colors.tertiary : colors.outlineVariant}`,
        padding: "14px 40px 14px 0", color: colors.onSurface, fontFamily: "Manrope", fontSize: 17,
        transition: "border-color 0.3s" }} />
    <span className="material-symbols-outlined absolute right-0 top-3.5" style={{ color: colors.tertiary }}>auto_awesome</span>
  </div>

  <div className="flex gap-6 mb-8 overflow-x-auto pb-0" style={{ borderBottom: "1px solid rgba(70,70,76,0.15)" }}>
    {filters.map((f) => (
      <button key={f} onClick={() => setFilter(f)}
        className="pb-4 whitespace-nowrap transition-all duration-300"
        style={{ background: "none", border: "none", cursor: "pointer", fontFamily: "Manrope", fontSize: 12,
          textTransform: "uppercase", letterSpacing: "0.1em",
          color: filter === f ? colors.tertiary : "rgba(227,225,234,0.5)",
          borderBottom: filter === f ? `2px solid ${colors.tertiary}` : "2px solid transparent" }}>
        {f === "all" ? "All Elements" : f.charAt(0).toUpperCase() + f.slice(1)}
      </button>
    ))}
  </div>

  <div className="flex flex-col gap-4">
    {filtered.map((it) => {
      if (it.layout === "hero") return (
        <div key={it.name} className="relative overflow-hidden rounded" style={{ height: 260, background: colors.surfaceLow }}>
          <img src={it.img} alt="" className="absolute inset-0 w-full h-full object-cover" style={{ opacity: 0.6 }} />
          <div className="absolute inset-0" style={{ background: "linear-gradient(to top,#121319,rgba(18,19,25,0.2) 40%,transparent)" }} />
          <div className="absolute bottom-0 left-0 p-6">
            <span style={{ fontFamily: "Manrope", fontSize: 10, textTransform: "uppercase", letterSpacing: "0.1em",
              color: colors.tertiary, background: "rgba(233,193,118,0.1)", border: "1px solid rgba(233,193,118,0.2)",
              padding: "4px 12px", display: "inline-block", marginBottom: 10 }}>{it.tag}</span>
            <h3 style={{ fontFamily: "'Noto Serif', serif", fontSize: 30, fontWeight: 700, marginBottom: 6, color: colors.onSurface }}>{it.name}</h3>
            <p style={{ color: colors.onSurfaceVariant, fontStyle: "italic", maxWidth: 360 }}>{it.desc}</p>
          </div>
        </div>
      );
      if (it.layout === "medium") return (
        <div key={it.name} className="overflow-hidden rounded" style={{ background: colors.surfaceContainer }}>
          <div style={{ height: 150, overflow: "hidden" }}>
            <img src={it.img} alt="" className="w-full h-full object-cover" style={{ opacity: 0.7 }} />
          </div>
          <div className="p-5">
            <h3 style={{ fontFamily: "'Noto Serif', serif", fontSize: 20, fontWeight: 700, marginBottom: 6, color: colors.onSurface }}>{it.name}</h3>
            <Label className="block mb-2" style={{ color: colors.onSurfaceVariant }}>{it.tag}</Label>
            <p style={{ fontSize: 14, color: "rgba(227,225,234,0.7)" }}>{it.desc}</p>
          </div>
        </div>
      );
      return (
        <div key={it.name} className="p-5 rounded transition-colors duration-300 hover:bg-[#292a30]"
          style={{ background: colors.surfaceLow, border: "1px solid rgba(70,70,76,0.1)" }}>
          <div className="w-full rounded overflow-hidden mb-4" style={{ aspectRatio: "1", background: colors.surfaceContainer }}>
            <img src={it.img} alt="" className="w-full h-full object-cover" style={{ opacity: 0.6 }} />
          </div>
          <h4 style={{ fontFamily: "'Noto Serif', serif", fontSize: 18, fontWeight: 700, marginBottom: 4, color: colors.onSurface }}>{it.name}</h4>
          <Label className="block mb-2" style={{ color: colors.tertiary, fontStyle: "italic" }}>{it.tag}</Label>
          <p style={{ fontSize: 13, color: colors.onSurfaceVariant }}>{it.desc}</p>
        </div>
      );
    })}
  </div>

  <div className="mt-8 p-8 rounded" style={{ background: "rgba(52,54,59,0.4)", backdropFilter: "blur(24px)",
    border: "1px solid rgba(70,70,76,0.15)" }}>
    <div className="flex justify-between items-start mb-6">
      <h3 style={{ fontFamily: "'Noto Serif', serif", fontSize: 21, fontWeight: 700, color: colors.onSurface }}>
        Weekly<br />Correspondence
      </h3>
      <span className="material-symbols-outlined" style={{ color: colors.tertiary, fontVariationSettings: "'FILL' 1" }}>star</span>
    </div>
    <p style={{ color: colors.onSurfaceVariant, fontSize: 14, fontStyle: "italic", marginBottom: 6 }}>Current Astral Alignment:</p>
    <p style={{ fontFamily: "'Noto Serif', serif", fontSize: 20, marginBottom: 14, color: colors.onSurface }}>Sage & Lapis Lazuli</p>
    <div style={{ height: 1, background: "rgba(70,70,76,0.2)", marginBottom: 14 }} />
    <p style={{ fontSize: 13, color: "rgba(199,197,204,0.7)", lineHeight: 1.7 }}>
      During this lunar phase, the combination of Sage and Lapis Lazuli enhances visionary work and linguistic precision.
    </p>
  </div>
</div>
```

);
};

// ─── GUIDES PAGE ───
const GuidesPage = () => {
const [seconds, setSeconds] = useState(671);
const [running, setRunning] = useState(false);
const [checked, setChecked] = useState({ 1: true });
const intervalRef = useRef(null);
const totalTime = 671;

useEffect(() => {
if (running && seconds > 0) {
intervalRef.current = setInterval(() => setSeconds((s) => s - 1), 1000);
} else {
clearInterval(intervalRef.current);
if (seconds <= 0) setRunning(false);
}
return () => clearInterval(intervalRef.current);
}, [running, seconds]);

const mins = Math.floor(seconds / 60);
const secs = seconds % 60;
const progress = ((totalTime - seconds) / totalTime) * 283;

const essentials = [
{ name: “Silver Candle”, note: “Charged under the full moon” },
{ name: “Obsidian Shard”, note: “To anchor the intent” },
{ name: “Dried Sage”, note: “For energetic purification” },
{ name: “Pure Water Source”, note: “Ideally from a moving stream” },
];

return (
<div className="pt-20 pb-28 px-6 animate-[fadeIn_0.4s_ease-out]">
<div className="text-center mb-10">
<span className=“inline-block mb-5” style={{ fontFamily: “Manrope”, fontSize: 10, textTransform: “uppercase”,
letterSpacing: “0.2em”, color: colors.tertiary, border: “1px solid rgba(70,70,76,0.15)”, padding: “6px 16px” }}>
Active Working
</span>
<h2 style={{ fontFamily: “‘Noto Serif’, serif”, fontSize: “clamp(34px,7vw,48px)”, letterSpacing: “-0.02em”,
marginBottom: 16, color: colors.onSurface }}>
Celestial Alignment Ritual
</h2>
<p style={{ color: “rgba(227,225,234,0.8)”, fontSize: 15, fontStyle: “italic”, lineHeight: 1.6, maxWidth: 440, margin: “0 auto” }}>
To harness the shifting tides of the lunar cycle and manifest clarity within the digital noise.
</p>
</div>

```
  <div className="flex items-center gap-4 p-4 rounded mb-8" style={{ background: colors.surfaceLow }}>
    <div className="w-12 h-12 rounded flex items-center justify-center" style={{ background: colors.surfaceHighest }}>
      <span className="material-symbols-outlined" style={{ color: colors.tertiary, fontVariationSettings: "'FILL' 1" }}>nights_stay</span>
    </div>
    <div>
      <Label className="block" style={{ color: colors.onSurfaceVariant }}>Current Phase</Label>
      <span style={{ fontWeight: 500, color: colors.onSurface }}>Waxing Gibbous</span>
    </div>
  </div>

  <div className="p-7 rounded mb-8" style={{ background: colors.surfaceLow }}>
    <h3 className="flex items-center gap-2 mb-6" style={{ fontFamily: "'Noto Serif', serif", fontSize: 19, color: colors.onSurface }}>
      <span style={{ width: 6, height: 6, background: colors.tertiary, display: "inline-block" }} />
      Casting Essentials
    </h3>
    {essentials.map((e, i) => (
      <div key={i} onClick={() => setChecked((c) => ({ ...c, [i]: !c[i] }))}
        className="flex items-start gap-4 p-2 -mx-2 mb-4 rounded cursor-pointer transition-colors duration-300 hover:bg-[#292a30]">
        <div className="flex-shrink-0 mt-0.5" style={{ width: 20, height: 20, borderRadius: 2,
          border: `1px solid ${checked[i] ? colors.tertiary : colors.outlineVariant}`,
          background: checked[i] ? "rgba(233,193,118,0.1)" : colors.surfaceLowest,
          display: "flex", alignItems: "center", justifyContent: "center", transition: "all 0.2s" }}>
          {checked[i] && <span className="material-symbols-outlined" style={{ fontSize: 14, color: colors.tertiary }}>check</span>}
        </div>
        <div style={{ opacity: checked[i] ? 0.4 : 1, transition: "opacity 0.3s" }}>
          <span style={{ fontWeight: 500, display: "block", textDecoration: checked[i] ? "line-through" : "none", color: colors.onSurface }}>{e.name}</span>
          <span style={{ fontSize: 12, color: "rgba(227,225,234,0.5)" }}>{e.note}</span>
        </div>
      </div>
    ))}
  </div>

  <div className="rounded overflow-hidden mb-8 relative" style={{ height: 180, filter: "grayscale(0.5)" }}>
    <img src={IMG.altar} alt="" className="w-full h-full object-cover" style={{ opacity: 0.6 }} />
    <div className="absolute inset-0" style={{ background: "linear-gradient(to top,#121319,transparent)" }} />
    <Label className="absolute bottom-4 left-4" style={{ color: colors.tertiary }}>Visual Focus</Label>
  </div>

  <div className="flex flex-col items-center p-10 rounded mb-8"
    style={{ background: colors.surfaceLow, border: "1px solid rgba(70,70,76,0.05)",
      backgroundImage: "radial-gradient(circle,rgba(233,193,118,0.08) 0%,transparent 70%)" }}>
    <div className="relative" style={{ width: 200, height: 200 }}>
      <svg width="200" height="200" className="absolute inset-0">
        <circle cx="100" cy="100" r="90" fill="none" stroke="rgba(70,70,76,0.2)" strokeWidth="2" />
        <circle cx="100" cy="100" r="90" fill="none" stroke={colors.tertiary} strokeWidth="3" strokeLinecap="round"
          style={{ strokeDasharray: 283, strokeDashoffset: 283 - progress,
            transform: "rotate(-90deg)", transformOrigin: "50% 50%", transition: "stroke-dashoffset 1s linear" }} />
      </svg>
      <div className="absolute inset-0 flex flex-col items-center justify-center">
        <span style={{ fontFamily: "'Noto Serif', serif", fontSize: 40, letterSpacing: "0.1em", color: colors.onSurface }}>
          {String(mins).padStart(2, "0")}:{String(secs).padStart(2, "0")}
        </span>
        <Label style={{ color: colors.tertiary, marginTop: 6 }}>Ritual Focus</Label>
      </div>
    </div>
    <div className="flex gap-4 mt-8">
      <button onClick={() => setRunning(!running)}
        style={{ padding: "10px 28px", background: colors.tertiary, color: colors.onTertiary, border: "none",
          cursor: "pointer", fontFamily: "Manrope", fontSize: 11, fontWeight: 600, textTransform: "uppercase",
          letterSpacing: "0.1em", boxShadow: running ? "0 0 25px rgba(233,193,118,0.35)" : "0 0 15px rgba(233,193,118,0.2)",
          transition: "all 0.3s" }}>
        {running ? "Pause" : "Begin"}
      </button>
      <button onClick={() => { setRunning(false); setSeconds(totalTime); }}
        style={{ padding: "10px 28px", background: "transparent", border: "1px solid rgba(70,70,76,0.15)",
          color: colors.onSurface, cursor: "pointer", fontFamily: "Manrope", fontSize: 11, textTransform: "uppercase",
          letterSpacing: "0.1em" }}>
        Reset
      </button>
    </div>
  </div>

  <div className="p-7 rounded mb-8" style={{ background: colors.surfaceLowest, border: "1px solid rgba(70,70,76,0.1)" }}>
    <h4 style={{ fontFamily: "'Noto Serif', serif", fontSize: 18, marginBottom: 10, color: colors.onSurface }}>The Weaver's Breath</h4>
    <p style={{ color: colors.onSurfaceVariant, fontSize: 14, lineHeight: 1.7 }}>
      As the timer counts, focus on the rhythmic expansion of your lungs. Imagine the breath as a silken thread, weaving through the objects on your altar, binding your intention to the physical world.
    </p>
    <div className="flex items-center gap-3 mt-5" style={{ color: "rgba(233,193,118,0.6)" }}>
      <span className="material-symbols-outlined">auto_fix_high</span>
      <Label>Active Intention Active</Label>
    </div>
  </div>

  <div className="p-8 rounded text-center" style={{ background: colors.surfaceLow, borderTop: "4px solid rgba(233,193,118,0.2)" }}>
    <span className="material-symbols-outlined block mx-auto mb-4" style={{ color: colors.tertiary, fontSize: 34 }}>history_edu</span>
    <h3 style={{ fontFamily: "'Noto Serif', serif", fontSize: 21, marginBottom: 6, color: colors.onSurface }}>The Spoken Incantation</h3>
    <Label className="block mb-7" style={{ color: colors.onSurfaceVariant }}>Recite with intentionality</Label>
    <p style={{ fontFamily: "'Noto Serif', serif", fontSize: "clamp(18px,4.5vw,26px)", lineHeight: 1.55, color: colors.onSurface }}>
      "By moon's bright face and shadow's deep,<br />
      Awaken what within me sleep.<br />
      From digital noise to sacred light,<br />
      I claim my voice within the night."
    </p>
    <div className="mt-7 pt-6" style={{ borderTop: "1px solid rgba(70,70,76,0.1)" }}>
      <p style={{ fontSize: 14, color: "rgba(227,225,234,0.6)", fontStyle: "italic", marginBottom: 16 }}>
        Repeat three times, allowing the final syllable of the third recitation to resonate until the timer concludes.
      </p>
      <div className="flex flex-wrap justify-center gap-2">
        {["Phase 1: Whisper", "Phase 2: Resonance", "Phase 3: Conviction"].map((p) => (
          <span key={p} style={{ padding: "6px 14px", background: colors.surfaceHighest, fontFamily: "Manrope",
            fontSize: 9, textTransform: "uppercase", letterSpacing: "0.1em", borderRadius: 16, color: colors.onSurface }}>{p}</span>
        ))}
      </div>
    </div>
  </div>
</div>
```

);
};

// ─── APP ───
export default function App() {
const [page, setPage] = useState(“library”);
const navigate = useCallback((p) => { setPage(p); window.scrollTo(0, 0); }, []);

const renderPage = () => {
switch (page) {
case “library”: return <LibraryPage onNavigate={navigate} />;
case “rituals”: return <RitualsPage />;
case “correspondence”: return <CorrespondencePage />;
case “guides”: return <GuidesPage />;
default: return <LibraryPage onNavigate={navigate} />;
}
};

return (
<div style={{ background: colors.bg, color: colors.onSurface, fontFamily: “‘Manrope’, sans-serif”, minHeight: “100vh” }}>
<link href="https://fonts.googleapis.com/css2?family=Noto+Serif:ital,wght@0,400;0,700;1,400&family=Manrope:wght@300;400;500;600;700;800&display=swap" rel="stylesheet" />
<link href="https://fonts.googleapis.com/css2?family=Material+Symbols+Outlined:wght,FILL@100..700,0..1&display=swap" rel="stylesheet" />
<style>{`.material-symbols-outlined { font-variation-settings: 'FILL' 0, 'wght' 300, 'GRAD' 0, 'opsz' 24; } ::-webkit-scrollbar { width: 4px; } ::-webkit-scrollbar-track { background: #121319; } ::-webkit-scrollbar-thumb { background: #46464c; } @keyframes fadeIn { from { opacity: 0; } to { opacity: 1; } }`}</style>
<div className=“fixed inset-0 pointer-events-none” style={{ zIndex: -1, overflow: “hidden” }}>
<div className=“absolute rounded-full” style={{ top: “25%”, left: -80, width: 300, height: 300, background: “rgba(233,193,118,0.04)”, filter: “blur(120px)” }} />
<div className=“absolute rounded-full” style={{ bottom: “25%”, right: -80, width: 260, height: 260, background: “rgba(196,197,219,0.04)”, filter: “blur(100px)” }} />
</div>
<Header />
<main className="max-w-3xl mx-auto">
{renderPage()}
</main>
<BottomNav active={page} onChange={navigate} />
</div>
);
}
